[package]
name = "rew"
version = "1.0.0"
edition = "2024"

[dependencies]

[profile.release]
codegen-units = 1
panic = "abort"
strip = true
lto = true

[lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
